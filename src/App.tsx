import React from 'react';
import styled from 'styled-components';

const App: React.FC = () => {
  return (
    <Container>
      Labyrinth
    </Container>
  );
}

const Container = styled.div`
  text-align: center;
`;

export default App;
